body {
  margin: 0;
  padding: 0;
  background: #ffffff;
  font-family: sans-serif;

  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
}

/* 学習開始ボタン */
#startBtn {
  width: 80%;
  max-width: 360px;
  height: 64px;

  background: #2a4d9b;
  color: #ffffff;

  font-size: 22px;
  font-weight: bold;

  border: none;
  border-radius: 32px;   /* 左右が丸くなる */
  cursor: pointer;
}

/* 押した感 */
#startBtn:active {
  opacity: 0.85;
}
